# 部署指南：Google Cloud Run

**生成時間**: {{TIMESTAMP}}
**專案**: {{PROJECT_NAME}}
**版本**: {{VERSION}}

---

## 📋 概述

此專案將部署到 **Google Cloud Run**，一個全託管的 serverless 容器平台。

### 部署資訊

- **GCP Project ID**: {{PROJECT_ID}}
- **地區**: {{REGION}}
- **服務名稱**: {{SERVICE_NAME}}
- **Container Port**: {{PORT}}

### Cloud Run 特性

✅ **自動擴展**: 根據流量自動調整實例數（{{MIN_INSTANCES}} - {{MAX_INSTANCES}}）
✅ **付費模式**: 按實際使用付費（請求數 + CPU/Memory 時間）
✅ **HTTPS**: 自動提供 HTTPS 端點
✅ **全球部署**: 支援多地區部署
✅ **零停機更新**: Rolling update

---

## 🔧 前置需求

### 1. GCP 帳號

如果還沒有：
1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
2. 建立新帳號（有 $300 免費額度）
3. 建立新專案或使用現有專案

### 2. 安裝 gcloud CLI

#### macOS

```bash
# 使用 Homebrew
brew install --cask google-cloud-sdk

# 或下載安裝包
curl https://sdk.cloud.google.com | bash
exec -l $SHELL
```

#### Linux

```bash
curl https://sdk.cloud.google.com | bash
exec -l $SHELL
```

#### Windows

下載並執行安裝程式：
https://cloud.google.com/sdk/docs/install

### 3. 初始化 gcloud

```bash
# 登入
gcloud auth login

# 設定專案
gcloud config set project {{PROJECT_ID}}

# 設定預設地區
gcloud config set run/region {{REGION}}
```

### 4. 啟用必要的 APIs

```bash
# 自動啟用（deploy.sh 會檢查並啟用）
# 或手動啟用：
gcloud services enable cloudbuild.googleapis.com
gcloud services enable run.googleapis.com
gcloud services enable containerregistry.googleapis.com
```

---

## 🚀 快速部署

### 方式 1: 使用部署腳本（推薦）

```bash
# 賦予執行權限
chmod +x deployment/deploy.sh

# 執行部署
./deployment/deploy.sh
```

這個腳本會：
1. ✅ 檢查 gcloud CLI
2. ✅ 檢查並啟用必要的 APIs
3. ✅ 顯示部署配置
4. ✅ 確認後開始建置
5. ✅ 使用 Cloud Build 建置 Docker image
6. ✅ 部署到 Cloud Run
7. ✅ 顯示服務 URL 和狀態

### 方式 2: 手動部署

```bash
# 1. 建置並推送 Docker image
docker build -t gcr.io/{{PROJECT_ID}}/{{SERVICE_NAME}} -f deployment/Dockerfile .
docker push gcr.io/{{PROJECT_ID}}/{{SERVICE_NAME}}

# 2. 部署到 Cloud Run
gcloud run deploy {{SERVICE_NAME}} \
  --image gcr.io/{{PROJECT_ID}}/{{SERVICE_NAME}} \
  --region {{REGION}} \
  --platform managed \
  --allow-unauthenticated \
  {{#if MIN_INSTANCES}}--min-instances {{MIN_INSTANCES}} \{{/if}}
  {{#if MAX_INSTANCES}}--max-instances {{MAX_INSTANCES}} \{{/if}}
  {{#if CPU}}--cpu {{CPU}} \{{/if}}
  {{#if MEMORY}}--memory {{MEMORY}} \{{/if}}
  --port {{PORT}}
```

### 方式 3: 使用 Cloud Build

```bash
gcloud builds submit --config=deployment/cloudbuild.yaml .
```

---

## 📦 部署配置詳解

### Dockerfile

位置: `deployment/Dockerfile`

**特點**:
- Multi-stage build（減少最終 image 大小）
- Non-root user（安全性）
- Health check（自動檢測服務狀態）

### cloudbuild.yaml

位置: `deployment/cloudbuild.yaml`

**建置步驟**:
1. Build Docker image
2. Push to Container Registry
3. Deploy to Cloud Run

**建置時間**: 通常 2-5 分鐘（首次較久，後續會快）

### 資源配置

在 `design-spec.yaml` 中定義：

```yaml
deployment:
  gcp:
    cloud_run:
      min_instances: {{MIN_INSTANCES}}  # 最小實例數（0 = 完全 serverless）
      max_instances: {{MAX_INSTANCES}}  # 最大實例數
      cpu: {{CPU}}                      # CPU 數量 (1, 2, 4, 8)
      memory: {{MEMORY}}                # 記憶體 (128Mi - 4Gi)
      concurrency: {{CONCURRENCY}}      # 每個實例同時處理請求數
      timeout: {{TIMEOUT}}              # 請求超時時間（秒）
```

---

## 🔐 環境變數與 Secrets

### 設定環境變數

**方式 1: 部署時設定**

```bash
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --set-env-vars NODE_ENV=production,LOG_LEVEL=info
```

**方式 2: 使用 .env.yaml**

```yaml
# .env.yaml
NODE_ENV: production
LOG_LEVEL: info
API_BASE_URL: https://api.example.com
```

```bash
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --env-vars-file .env.yaml
```

### 設定 Secrets (Secret Manager)

**步驟 1: 建立 secret**

```bash
# 建立 secret
echo -n "your-secret-value" | gcloud secrets create DATABASE_URL --data-file=-

# 或從檔案
gcloud secrets create API_KEY --data-file=api-key.txt
```

**步驟 2: 授權 Cloud Run 存取**

```bash
# 取得 service account
SERVICE_ACCOUNT=$(gcloud run services describe {{SERVICE_NAME}} \
  --region {{REGION}} \
  --format='value(spec.template.spec.serviceAccountName)')

# 授權
gcloud secrets add-iam-policy-binding DATABASE_URL \
  --member=serviceAccount:$SERVICE_ACCOUNT \
  --role=roles/secretmanager.secretAccessor
```

**步驟 3: 設定 Cloud Run 使用 secret**

```bash
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --set-secrets=DATABASE_URL=DATABASE_URL:latest
```

在程式碼中使用：

```javascript
// Node.js
const databaseUrl = process.env.DATABASE_URL;

// Python
import os
database_url = os.getenv('DATABASE_URL')
```

---

## 🗄️  資料庫連線

### 連接 Cloud SQL

**步驟 1: 建立 Cloud SQL 實例**

```bash
gcloud sql instances create my-postgres \
  --database-version=POSTGRES_15 \
  --tier=db-f1-micro \
  --region={{REGION}}
```

**步驟 2: 建立資料庫**

```bash
gcloud sql databases create mydb --instance=my-postgres
```

**步驟 3: 連接 Cloud Run 到 Cloud SQL**

```bash
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --add-cloudsql-instances {{PROJECT_ID}}:{{REGION}}:my-postgres
```

**步驟 4: 設定連線字串**

```bash
# Unix socket 連線（推薦）
CONNECTION_NAME={{PROJECT_ID}}:{{REGION}}:my-postgres
DB_URL="postgresql://user:password@/mydb?host=/cloudsql/$CONNECTION_NAME"

gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --set-env-vars DATABASE_URL="$DB_URL"
```

### 連接外部資料庫

如果使用外部資料庫（如 Supabase、PlanetScale）：

```bash
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --set-secrets=DATABASE_URL=DATABASE_URL:latest
```

---

## 📊 監控與日誌

### 查看日誌

**即時日誌**:

```bash
gcloud logging tail "resource.type=cloud_run_revision AND resource.labels.service_name={{SERVICE_NAME}}"
```

**歷史日誌**:

```bash
gcloud logging read \
  "resource.type=cloud_run_revision AND resource.labels.service_name={{SERVICE_NAME}}" \
  --limit 50 \
  --format json
```

**在 Console 查看**:

https://console.cloud.google.com/logs/query?project={{PROJECT_ID}}

### 監控指標

**在 Cloud Console 查看**:

https://console.cloud.google.com/run/detail/{{REGION}}/{{SERVICE_NAME}}/metrics

**指標包含**:
- 請求數
- 請求延遲
- 實例數
- CPU 使用率
- 記憶體使用率
- 錯誤率

### 設定告警

```bash
# 建立告警政策（當錯誤率 > 5% 時通知）
gcloud alpha monitoring policies create \
  --notification-channels=CHANNEL_ID \
  --display-name="{{SERVICE_NAME}} High Error Rate" \
  --condition-display-name="Error rate > 5%" \
  --condition-threshold-value=5 \
  --condition-threshold-duration=60s
```

---

## 🔄 更新與回滾

### 部署新版本

```bash
# 方式 1: 重新執行部署腳本
./deployment/deploy.sh

# 方式 2: 直接更新 image
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --image gcr.io/{{PROJECT_ID}}/{{SERVICE_NAME}}:new-tag
```

### 查看版本歷史

```bash
gcloud run revisions list \
  --service {{SERVICE_NAME}} \
  --region {{REGION}}
```

### 回滾到上一個版本

```bash
# 1. 找出上一個 revision
PREVIOUS_REVISION=$(gcloud run revisions list \
  --service {{SERVICE_NAME}} \
  --region {{REGION}} \
  --format="value(metadata.name)" \
  --limit=2 | tail -1)

# 2. 切換流量到上一個 revision
gcloud run services update-traffic {{SERVICE_NAME}} \
  --region {{REGION}} \
  --to-revisions $PREVIOUS_REVISION=100
```

### 金絲雀部署（Canary Deployment）

```bash
# 將 10% 流量導向新版本，90% 維持舊版本
gcloud run services update-traffic {{SERVICE_NAME}} \
  --region {{REGION}} \
  --to-revisions new-revision=10,old-revision=90

# 確認無問題後，逐步增加新版本流量
gcloud run services update-traffic {{SERVICE_NAME}} \
  --region {{REGION}} \
  --to-revisions new-revision=50,old-revision=50

# 最終完全切換到新版本
gcloud run services update-traffic {{SERVICE_NAME}} \
  --region {{REGION}} \
  --to-latest
```

---

## 🌍 自訂網域

### 設定自訂網域

**步驟 1: 驗證網域擁有權**

在 [Google Search Console](https://search.google.com/search-console) 驗證網域。

**步驟 2: 映射網域到 Cloud Run**

```bash
gcloud run domain-mappings create \
  --service {{SERVICE_NAME}} \
  --domain api.yourdomain.com \
  --region {{REGION}}
```

**步驟 3: 設定 DNS**

在你的 DNS 提供商設定：

```
CNAME api.yourdomain.com → ghs.googlehosted.com
```

或使用 Cloud DNS：

```bash
gcloud dns record-sets transaction start --zone=my-zone
gcloud dns record-sets transaction add ghs.googlehosted.com \
  --name=api.yourdomain.com \
  --ttl=300 \
  --type=CNAME \
  --zone=my-zone
gcloud dns record-sets transaction execute --zone=my-zone
```

**步驟 4: 等待 SSL 憑證**

通常需要 15-30 分鐘，Google 會自動配置 SSL 憑證。

---

## 💰 成本估算

### 定價模型

Cloud Run 收費項目：
1. **CPU 時間**: 處理請求時使用的 CPU
2. **記憶體時間**: 處理請求時使用的記憶體
3. **請求數**: 總請求數

### 免費額度（每月）

- CPU: 2 百萬 vCPU-秒
- 記憶體: 360,000 GiB-秒
- 請求數: 2 百萬次

### 估算工具

使用 [GCP Pricing Calculator](https://cloud.google.com/products/calculator)

**範例估算**（中小型應用）:

```
設定:
- 請求數: 100,000 次/月
- 平均回應時間: 200ms
- Memory: 512Mi
- CPU: 1

估算費用: ~$5-10/月
```

### 優化成本

1. **降低 min_instances**:
   ```bash
   # 設為 0（完全 serverless）
   gcloud run services update {{SERVICE_NAME}} --min-instances 0
   ```

2. **減少記憶體**:
   ```bash
   gcloud run services update {{SERVICE_NAME}} --memory 256Mi
   ```

3. **啟用 request timeout**:
   ```bash
   gcloud run services update {{SERVICE_NAME}} --timeout 60
   ```

4. **使用 CPU 分配模式**:
   ```bash
   # 只在處理請求時分配 CPU
   gcloud run services update {{SERVICE_NAME}} --cpu-throttling
   ```

---

## 🔒 安全性

### 啟用身份驗證

**只允許已授權的使用者存取**:

```bash
# 移除 allow-unauthenticated
gcloud run services remove-iam-policy-binding {{SERVICE_NAME}} \
  --region {{REGION}} \
  --member="allUsers" \
  --role="roles/run.invoker"

# 加入特定使用者
gcloud run services add-iam-policy-binding {{SERVICE_NAME}} \
  --region {{REGION}} \
  --member="user:alice@example.com" \
  --role="roles/run.invoker"
```

**使用 Service Account 呼叫**:

```bash
# 取得 ID token
ID_TOKEN=$(gcloud auth print-identity-token)

# 使用 token 呼叫 API
curl -H "Authorization: Bearer $ID_TOKEN" \
  https://{{SERVICE_NAME}}-xxx.a.run.app
```

### VPC Connector

連接私有網路資源：

```bash
# 建立 VPC connector
gcloud compute networks vpc-access connectors create my-connector \
  --region {{REGION}} \
  --range 10.8.0.0/28

# 設定 Cloud Run 使用
gcloud run services update {{SERVICE_NAME}} \
  --region {{REGION}} \
  --vpc-connector my-connector
```

### 防火牆規則

Cloud Run 預設對外公開，可以在應用程式層實作：
- IP whitelist
- Rate limiting
- API key 驗證
- OAuth/JWT 驗證

---

## 🐛 故障排除

### 部署失敗

#### Error: Permission denied

**解決**:
```bash
# 確認有正確的 IAM 角色
gcloud projects add-iam-policy-binding {{PROJECT_ID}} \
  --member="user:your-email@example.com" \
  --role="roles/run.admin"
```

#### Error: Container failed to start

**檢查**:
1. 查看建置日誌: `gcloud builds list`
2. 查看 Cloud Run 日誌: `gcloud logging read ...`
3. 確認 PORT 環境變數正確使用

**常見原因**:
- App 沒有監聽 PORT 環境變數
- Health check 失敗
- 啟動時間過長（> 10 分鐘）

### 服務無回應

#### HTTP 502/503 錯誤

**檢查**:
```bash
# 查看實例狀態
gcloud run services describe {{SERVICE_NAME}} --region {{REGION}}

# 查看日誌
gcloud logging read "resource.type=cloud_run_revision" --limit 50
```

**常見原因**:
- 記憶體不足（OOM）
- CPU 不足
- 請求超時

**解決**:
```bash
# 增加資源
gcloud run services update {{SERVICE_NAME}} \
  --memory 1Gi \
  --cpu 2
```

### 效能問題

#### Cold start 太慢

**解決**:
```bash
# 設定 min_instances > 0
gcloud run services update {{SERVICE_NAME}} --min-instances 1
```

#### 請求延遲高

**檢查**:
1. 查看 Cloud Run metrics
2. 檢查資料庫查詢效能
3. 檢查外部 API 呼叫

**優化**:
- 增加 concurrency
- 啟用 HTTP/2
- 使用 CDN（Cloud CDN）

---

## 📚 相關資源

### 官方文件

- [Cloud Run 文件](https://cloud.google.com/run/docs)
- [Cloud Build 文件](https://cloud.google.com/build/docs)
- [Secret Manager 文件](https://cloud.google.com/secret-manager/docs)

### 最佳實踐

- [Cloud Run 最佳實踐](https://cloud.google.com/run/docs/tips)
- [容器最佳實踐](https://cloud.google.com/architecture/best-practices-for-building-containers)

### 範例專案

- [Cloud Run Samples](https://github.com/GoogleCloudPlatform/cloud-run-samples)

---

## 📝 檢查清單

部署前確認：

- [ ] gcloud CLI 已安裝並登入
- [ ] 專案 ID 正確
- [ ] 必要的 APIs 已啟用
- [ ] Dockerfile 正確建置
- [ ] 環境變數已設定
- [ ] Secrets 已建立並授權
- [ ] 資料庫連線正常（如果需要）
- [ ] Health check 端點回應 200
- [ ] 本地測試通過
- [ ] 已準備監控和告警
- [ ] 已了解成本估算

---

**生成工具**: SpecKit V2 - `/speckit.deploy`
**生成時間**: {{TIMESTAMP}}
**文檔版本**: 1.0.0
